# Build artifacts — never commit binaries
/rowtr
/rowtr-tray
/bin/
/dist/
cmd/rowtr/rowtr
*.exe
*.test
coverage.out
*.prof

# Project memory / AI-session files — local only
CLAUDE.md
.claude/

# Local logs / scratch
*.log

# Secrets & local state — defense in depth (these live in the OS config dir,
# but must never land in the repo even by accident)
.env
*.pem
*.key
proxy-token
usage.db*

# Editor / OS noise
.DS_Store
.vscode/
.idea/
.zshrc
